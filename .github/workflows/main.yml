name: TryPipeline
on: 
  [push, workflow_dispatch]
  
jobs:
  bestHero:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "CJ"

  grandTheftAuto:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "Michael, Franklin, Trevor"
